const btn2 = document.getElementById("btn-2");
const btn3 = document.getElementById("btn-3");

function changeContent() {
    // B1 : Tạo 1 mảng quote

    // B2 : Random 1 phần tử bất kỳ trong mảng

    // B3 : Gán lại nội dung cho thẻ p
}

btn2.onclick = function() {
    // B1 : Random màu hex (có thể viết function random bên ngoài, sau đó gọi ở đây)

    // B2 : Thay đổi màu chữ cho thẻ p
}

btn3.addEventListener("click", function() {
    // B1 : Random màu rgb (có thể viết function random bên ngoài, sau đó gọi ở đây)

    // B2 : Thay đổi màu background cho thẻ p
})