const contentMDE = new EasyMDE({element: document.getElementById('content')});
const descriptionMDE = new EasyMDE({
    element: document.getElementById('description'),
    maxHeight : "100px",
    hideIcons : true
});